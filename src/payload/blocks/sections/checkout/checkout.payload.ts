import { Block } from 'payload';

const checkoutSectionPayloadConfig = {
  slug: 'checkout',
  interfaceName: 'CheckoutBlock',
  fields: [],
} satisfies Block;

export { checkoutSectionPayloadConfig };
